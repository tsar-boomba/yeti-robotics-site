import styled from 'styled-components';

export const GridStyle = styled.div`
	display: flex;
	flex-direction: row;
	justify-content: center;
	flex-wrap: wrap;
	width: clamp(300px, 1900px, 100%);
	margin: 3rem 0 0 0;
`;
